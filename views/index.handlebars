<h1>Burgers!</h1>

<h2>Burgers have not been eaten!</h2>

<ul id = "notdevoured">
  {{#each burger}}
    {{#unless devoured}}
     <li>{{this.burgerName}}</li>
     <button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">Eat Burger!
		{{#if devoured}}EATEN!{{/if}}
	</button>

    {{/unless}}
  {{/each}}
</ul>

<h2>Burger was devoured!</h2>

<ul id="beendevoured">
  {{#each burger}}
    {{#if devoured}}
     <li> {{this.burgerName}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit">Add Burger</button>
</form>