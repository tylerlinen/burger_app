<h1>Burgers!</h1>

<h2>Burgers have not been eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
     <li> {{this.burgerName}}</li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger was devoured!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
     <li> {{this.id}}.  {{this.burgerName}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit">Add Burger</button>
</form>